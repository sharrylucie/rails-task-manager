<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= check_box_tag :accept, 1, @task.completed %>

  <%= f.submit %>
<% end %>
<%= link_to "Back to list", index_path %>
